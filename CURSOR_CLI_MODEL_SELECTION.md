# Какая модель LLM используется в Cursor CLI и как её менять?

## Краткий ответ

Когда ты отправляешь запрос в Cursor через Tayfa:

**Модель берётся из `employees.json`** для конкретного агента в поле `"model"`.

```json
{
  "developer": {
    "role": "Developer",
    "model": "opus"          // ← Эта модель используется и в Claude, и в Cursor
  }
}
```

**Когда Cursor получает запрос**, он выполняет команду:
```bash
agent -p --force --resume '<chat_id>' --model 'opus' --output-format json "prompt"
```

Флаг `--model` передаётся в Cursor CLI, и **Cursor сам решает**, какую модель использовать на основе переданного значения.

---

## 📋 Как это работает

### 1. Модель в employees.json

Каждый агент имеет field `"model"`:

```json
{
  "employees": {
    "boss": {
      "role": "Project Manager",
      "model": "opus"           // ← Claude Opus
    },
    "developer": {
      "role": "Developer",
      "model": "opus"           // ← Claude Opus (сложные задачи)
    },
    "tester": {
      "role": "QA Engineer",
      "model": "sonnet"         // ← Claude Sonnet (средние задачи)
    },
    "reviewer": {
      "role": "Code Reviewer",
      "model": "haiku"          // ← Claude Haiku (простые задачи)
    }
  }
}
```

### 2. Поток выбора модели в Cursor запросе

**В коде `kok/app.py`**:

```python
@app.post("/api/send-prompt-cursor")
async def send_prompt_cursor(data: dict):
    """Send to Cursor CLI. Model from employees.json or override via data.model"""

    name = data.get("name")  # "developer"
    prompt_text = data.get("prompt")

    # Шаг 1: Проверить переопределение модели в запросе
    model = data.get("model")

    # Шаг 2: Если нет переопределения — взять из employees.json
    if model is None or model == "":
        emp = _get_employees().get(name, {})
        model = emp.get("model") or None
        # model = "opus" (для developer)

    # Шаг 3: Передать модель в Cursor CLI
    result = await run_cursor_cli(name, prompt_text, use_chat=use_chat, model=model)
    # Выполнит: agent -p --force --model 'opus' --resume '<chat_id>' ...

    return result
```

**В функции `run_cursor_cli()`**:

```python
async def run_cursor_cli(agent_name: str, user_prompt: str, use_chat: bool = True, model: str | None = None) -> dict:
    """
    Runs Cursor CLI in WSL.
    model: optional model id (e.g. opus, sonnet, haiku or claude-4-opus)
    """

    # Построить флаг --model для bash команды
    model_part = _cursor_cli_model_flag(model)
    # model_part = " --model 'opus'" если model="opus"
    # model_part = "" если model is None

    # Построить WSL команду
    wsl_script = (
        f"{base} && "
        "content=$(cat .cursor_cli_prompt.txt | ...) && "
        f"agent -p --force{resume_part}{model_part} --output-format json \"$content\""
    )
    # Результат:
    # agent -p --force --resume '<chat_id>' --model 'opus' --output-format json "..."

    # Выполнить в WSL
    proc = await asyncio.create_subprocess_exec("wsl", "bash", ...)

    return {"success": True, "result": "..."}
```

---

## 🔄 Поток выбора модели

```
Tayfa UI: нажимаешь "Send" с runtime="cursor"
    ↓
POST /api/send-prompt-cursor {
    "name": "developer",
    "prompt": "...",
    "model": null              // Нет переопределения
}
    ↓
send_prompt_cursor():
  1. Проверить data.get("model") → None
  2. Загрузить employees.json
  3. Получить developer.model → "opus"
  4. model = "opus"
    ↓
run_cursor_cli(agent_name="developer", model="opus"):
  1. _cursor_cli_model_flag("opus") → " --model 'opus'"
  2. Построить команду: agent -p --force --model 'opus' --resume '<id>' ...
  3. Выполнить в WSL
    ↓
Cursor CLI в WSL:
  1. Получить флаг --model opus
  2. Запустить Composer model (или другую LLM) с этой инструкцией
  3. Вернуть результат в JSON
    ↓
Tayfa получает результат → показывает в UI
```

---

## 🎯 Как менять модель для агента

### Способ 1: Редактировать employees.json (постоянное изменение)

**Файл**: `.tayfa/common/employees.json`

**Было:**
```json
{
  "developer": {
    "role": "Developer",
    "model": "opus"
  }
}
```

**Стало:**
```json
{
  "developer": {
    "role": "Developer",
    "model": "sonnet"    // ← Меняем с opus на sonnet
  }
}
```

**Эффект**: Все последующие запросы (Claude и Cursor) будут использовать `sonnet`.

### Способ 2: Использовать CLI команду

```bash
# Обновить конфиг агента через employee_manager.py
python .tayfa/common/employee_manager.py register developer "Developer" --model sonnet
```

**Результат**: Обновится `employees.json` для агента `developer`.

### Способ 3: Переопределить модель в одном запросе (для API)

**Через REST API** (если ты вызываешь Tayfa программно):

```bash
curl -X POST http://localhost:8008/api/send-prompt-cursor \
  -H "Content-Type: application/json" \
  -d '{
    "name": "developer",
    "prompt": "Add error handling",
    "model": "haiku"            # ← Переопределяем модель только для этого запроса
  }'
```

**Эффект**: Этот запрос использует `haiku`, но `employees.json` не меняется.

---

## 📊 Таблица доступных моделей

### Для Claude API (когда runtime="claude")

| Model | Использование | Стоимость | Скорость |
|-------|---------------|-----------|----------|
| **opus** | Сложные задачи, архитектура | Дороговато | Медленнее |
| **sonnet** | Сбалансированные задачи | Средняя | Средняя |
| **haiku** | Простые задачи, код-ревью | Дешево | Быстро |

### Для Cursor CLI (когда runtime="cursor")

Когда ты отправляешь `--model opus` в Cursor CLI:

```bash
agent -p --force --model 'opus' ...
```

**Cursor интерпретирует это как:**
- `"opus"` → Claude 3.5 Opus (или то, что установлено в Cursor)
- `"sonnet"` → Claude 3.5 Sonnet
- `"haiku"` → Claude 3.5 Haiku
- `"claude-4-opus"` → Claude 4 Opus (если поддерживается)
- Любая другая строка → Cursor попытается использовать

⚠️ **Важно**: Cursor может иметь свой набор доступных моделей в зависимости от версии и конфига.

---

## 🔍 Пример: Как изменить модель для разработчика

### Задача
Ты хочешь, чтобы developer использовал **sonnet** вместо **opus** (чтобы было дешевле).

### Решение 1: Отредактировать JSON

```bash
# Открыть редактор
nano .tayfa/common/employees.json

# Найти "developer" и изменить "model": "opus" на "model": "sonnet"
```

### Решение 2: Использовать CLI

```bash
python .tayfa/common/employee_manager.py register developer "Developer" \
  --model sonnet \
  --permission-mode bypassPermissions \
  --allowed-tools "Read Edit Write Bash"
```

### Проверить результат

```bash
python .tayfa/common/employee_manager.py get developer

# Вывод:
# {
#   "role": "Developer",
#   "model": "sonnet",    # ← Изменилось
#   "created_at": "2026-02-16"
# }
```

### Как это повлияет на использование

**Когда используешь Claude runtime:**
```
Tayfa → POST /api/send-prompt → call_claude_api(..., model="sonnet") → Claude API
```

**Когда используешь Cursor runtime:**
```
Tayfa → POST /api/send-prompt-cursor → WSL → agent -p --model 'sonnet' ... → Cursor CLI
```

**В обоих случаях** используется модель **sonnet**.

---

## 🚀 Практические примеры

### Пример 1: Дешёвые быстрые запросы

```json
{
  "reviewer": {
    "role": "Code Reviewer",
    "model": "haiku"    // Быстро, дешево
  }
}
```

Когда reviewer отвечает:
- **Claude**: Haiku (0.08 сент за миллион входящих токенов)
- **Cursor**: Haiku (через CLI, бесплатно если есть подписка)

### Пример 2: Дорогие сложные запросы

```json
{
  "architect": {
    "role": "AI Agent Architecture Specialist",
    "model": "opus"     // Дорого, но очень умно
  }
}
```

Когда architect отвечает:
- **Claude**: Opus ($15 за миллион входящих токенов)
- **Cursor**: Opus (через CLI, бесплатно если есть подписка)

### Пример 3: Смешанная стратегия

```json
{
  "boss": {
    "role": "Project Manager",
    "model": "opus"         // Сложные решения
  },
  "developer": {
    "role": "Developer",
    "model": "sonnet"       // Сбалансированный выбор
  },
  "tester": {
    "role": "QA Engineer",
    "model": "sonnet"       // Достаточно для проверок
  },
  "reviewer": {
    "role": "Code Reviewer",
    "model": "haiku"        // Быстрые код-ревью
  }
}
```

**Стоимость в месяц (Claude API)**:
- Если все запросы в Claude: ~0.50-2.00 USD в месяц (в зависимости от объёма)
- Если все запросы в Cursor: Бесплатно (если есть подписка на Cursor)

---

## 🔗 Где модель используется в коде

### 1. Декларация (employees.json)
```json
"model": "opus"
```

### 2. Чтение (app.py, _agents_from_registry)
```python
model = emp_data.get("model", "sonnet")
```

### 3. Передача в Claude (app.py, send_prompt)
```python
# Через call_claude_api с параметром model
api_result = await call_claude_api("POST", "/run", json_data=data)
```

### 4. Передача в Cursor (app.py, send_prompt_cursor)
```python
model = data.get("model") or emp.get("model")
result = await run_cursor_cli(name, prompt_text, model=model)
# Генерирует: agent -p --force --model 'opus' ...
```

### 5. Отображение в UI (index.html)
```javascript
const agentRuntime = getAgentRuntime(agentName);  // "opus", "sonnet", "haiku", или "cursor"
const runtimeLabel = agentRuntime === 'cursor' ? 'Via Cursor CLI' : 'Via Claude API';
```

---

## ⚙️ Полный цикл запроса с моделью

```
┌─────────────────────────────────────────┐
│      Tayfa Web Interface                │
│                                         │
│  Agent: developer (model: opus)         │
│  Runtime: [Claude] [Cursor]             │
│  Message: "Add auth"  [Send]            │
└─────────────────────┬───────────────────┘
                      │
                      │ Выбрал "Cursor"
                      ▼
┌─────────────────────────────────────────┐
│  POST /api/send-prompt-cursor           │
│  {name: "developer", prompt: "..."}     │
│  (model не указана → возьмётся из JSON) │
└─────────────────────┬───────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────┐
│  Tayfa Orchestrator (app.py)            │
│                                         │
│  1. model = data.get("model") → None    │
│  2. emp = employees["developer"]        │
│  3. model = emp.get("model") → "opus"   │
│  4. model_part = " --model 'opus'"      │
└─────────────────────┬───────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────┐
│  WSL Command                            │
│                                         │
│  agent -p --force \                     │
│    --resume '<chat_id>' \               │
│    --model 'opus' \                     │
│    --output-format json \               │
│    "Role: developer. Task: Add auth"    │
└─────────────────────┬───────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────┐
│  Cursor CLI в WSL                       │
│                                         │
│  Получить --model 'opus'                │
│  Загрузить сохранённый чат <chat_id>   │
│  Запустить Composer model с opus        │
│  (или любую другую LLM Cursor'а)        │
│  Вернуть результат в JSON               │
└─────────────────────┬───────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────┐
│  Tayfa получает результат               │
│                                         │
│  {"success": true, "result": "..."}     │
│                                         │
│  Сохраняет в chat_history.json:         │
│  runtime: "cursor"                      │
│  model: "opus" (из результата)          │
└─────────────────────┬───────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────┐
│      Tayfa Web Interface                │
│                                         │
│  ✅ Agent response: "..."               │
│     Runtime: "Cursor CLI"               │
│     Model: Opus (implied)               │
└─────────────────────────────────────────┘
```

---

## 🔑 Ключевые моменты

### 1. **Одна конфигурация модели для обоих runtime**

```json
"model": "opus"
```

Это значение используется **и для Claude API, и для Cursor CLI**.

Когда переключаешься между runtime'ами в UI, модель остаётся той же.

### 2. **Cursor CLI принимает текстовые имена моделей**

```bash
agent --model 'opus'      # ✅ Работает
agent --model 'sonnet'    # ✅ Работает
agent --model 'haiku'     # ✅ Работает
agent --model 'claude-4'  # ❓ Может работать, может нет (зависит от версии Cursor)
```

### 3. **Можно переопределять модель на лету (для API)**

```bash
curl -X POST http://localhost:8008/api/send-prompt-cursor \
  -H "Content-Type: application/json" \
  -d '{"name": "developer", "prompt": "...", "model": "haiku"}'
```

### 4. **Модель влияет на стоимость (только Claude API, не Cursor)**

- **Claude**: Разные модели = разные цены
- **Cursor**: Все модели = бесплатно (если подписка)

---

## 📝 Резюме

| Вопрос | Ответ |
|--------|-------|
| **Какая модель в Cursor CLI?** | Из `employees.json` для агента (поле `"model"`) |
| **Какая по умолчанию?** | `sonnet` (если не указана) |
| **Как менять?** | Отредактировать `employees.json` или использовать `employee_manager.py` |
| **Работает ли в обоих runtime?** | Да, одна модель для Claude и Cursor |
| **Можно ли переопределять на лету?** | Да, через API (поле `model` в запросе) |
| **Cursor CLI использует эту модель?** | Да, через флаг `--model 'opus'` (или другое) |
| **Разные цены?** | Только в Claude API, в Cursor бесплатно |

**В итоге**: Tayfa интеллектуально использует одну конфигурацию модели для обоих backend'ов (Claude API и Cursor CLI), что делает систему гибкой и удобной.
