# Правила тестирования

## Обязательность автотестов

Автотесты — **обязательная часть Definition of Done**. Задача не считается выполненной, если:
- Код не покрыт автотестами (где это применимо)
- Существующие тесты не проходят

---

## Классификация тестов

### 🚀 Быстрые тесты (Fast Tests)

**Характеристика:** Не требуют запуска приложения, не взаимодействуют с UI, не мешают пользователю.

| Тип | Описание | Примеры |
|-----|----------|---------|
| Unit-тесты | Тестирование отдельных функций/классов | pytest, jest, vitest |
| Интеграционные (без UI) | Тестирование взаимодействия модулей | API-тесты, DB-тесты |
| Snapshot-тесты | Сравнение выходных данных с эталоном | React snapshot testing |

**Когда запускать:**
- ✅ На каждый коммит (CI/CD)
- ✅ Перед переводом задачи в `на_проверке`
- ✅ При локальной разработке

**Команда:** `npm test` / `pytest` / специфичная для проекта

---

### 🐢 Медленные тесты (Slow Tests)

**Характеристика:** Требуют запуска приложения, захватывают мышь/клавиатуру, **мешают пользователю работать**.

| Тип | Описание | Примеры |
|-----|----------|---------|
| E2E (end-to-end) | Полный пользовательский сценарий | Playwright, Cypress, Selenium |
| UI-автотесты | Автоматизация через интерфейс | pyautogui, robot framework |
| Visual regression | Скриншот-сравнение UI | Percy, Chromatic |

**Когда запускать:**
- ⚠️ По запросу (не автоматически)
- ⚠️ Перед релизом / финализацией спринта
- ⚠️ Ночью (scheduled CI)
- ❌ НЕ на каждый коммит

**Важно:** Медленные тесты **захватывают управление** — предупреждай пользователя!

```
⚠️ Внимание: Запускаю E2E-тесты. 
Тесты займут ~X минут и будут управлять мышью/клавиатурой.
Не трогайте компьютер до завершения.
```

---

## Структура тестов в проекте

```
tests/
├── fast/                 # Быстрые тесты
│   ├── unit/            # Unit-тесты
│   └── integration/     # Интеграционные (без UI)
└── slow/                 # Медленные тесты
    ├── e2e/             # End-to-end
    └── visual/          # Visual regression
```

Или через маркеры/теги в тестовом фреймворке:
- `@fast` / `@slow`
- `pytest -m "not slow"` — только быстрые
- `pytest -m slow` — только медленные

---

## Когда какие тесты писать

### При разработке новой фичи

| Этап | Тип тестов |
|------|-----------|
| Разработка | Unit-тесты на логику |
| Интеграция | Интеграционные тесты |
| Перед PR | Все быстрые тесты проходят |
| Перед релизом | E2E на критический путь |

### При исправлении бага

1. Напиши тест, который воспроизводит баг (должен падать)
2. Исправь баг
3. Тест должен проходить

---

## Управление тестовым набором

### Добавление новых тестов

Новые тесты **добавляются** в обязательный набор автоматически — просто положи в `tests/fast/`.

### Изменение/удаление старых тестов

Тесты **можно удалить или изменить** если:
- Функционал удалён из продукта
- Тест тестировал deprecated-поведение
- Тест был некорректным (false positive/negative)

**Процедура:**
1. Создай задачу "Ревизия тестов" с описанием причин
2. Получи подтверждение от qa_tester
3. Удали/измени тесты
4. Задокументируй в PR/коммите

### Flaky-тесты (нестабильные)

Тесты, которые проходят/падают случайным образом:
1. Помечаем `@flaky` или `@skip`
2. Создаём задачу на исправление
3. Либо исправляем, либо удаляем

**Нельзя:** игнорировать flaky-тесты без документирования.

---

## Чек-лист для qa_tester

При проверке задачи (`на_проверке`):

- [ ] Быстрые тесты проходят (`npm test` / `pytest`)
- [ ] Новый код покрыт тестами (если применимо)
- [ ] Существующие тесты не сломаны
- [ ] Если критическая фича — есть E2E-тест

При финализации спринта:

- [ ] Все быстрые тесты проходят
- [ ] Запустить медленные тесты (E2E)
- [ ] Visual regression (если есть)

---

## Интеграция с CI/CD

```yaml
# Пример GitHub Actions
on: [push, pull_request]

jobs:
  fast-tests:
    runs-on: ubuntu-latest
    steps:
      - run: npm test  # Быстрые тесты на каждый push

  slow-tests:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'  # Только при merge в main
    steps:
      - run: npm run test:e2e  # Медленные тесты
```

---

## Definition of Done (тесты)

Задача считается выполненной, если:

✅ Быстрые тесты проходят  
✅ Новый код покрыт unit-тестами  
✅ Существующие тесты не сломаны  
✅ При критических изменениях — E2E-тест добавлен/обновлён
