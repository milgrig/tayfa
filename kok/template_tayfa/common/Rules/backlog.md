# Бэклог задач

## 1. Введение

### Что такое бэклог

**Бэклог** — хранилище неоформленных идей, заметок и будущих задач. Это промежуточное звено между первоначальной идеей и формальной задачей в спринте.

Бэклог служит «копилкой идей»:
- Фиксируешь идею, чтобы не забыть
- Идея ещё сырая, требует проработки
- Функционал нужен в будущем, но не сейчас
- Нужно обсудить с командой перед реализацией

### Зачем нужен бэклог

**Проблема без бэклога:**
- Идеи теряются (записали в чат, забыли)
- Незрелые задачи попадают в спринт
- Нет централизованного места для сбора идей

**Решение с бэклогом:**
- ✅ Все идеи в одном месте
- ✅ Boss видит полную картину будущих задач
- ✅ Можно приоритизировать и планировать
- ✅ Легко перенести в спринт когда придёт время

### Отличие бэклога от задач

| Бэклог | Задача |
|--------|--------|
| Черновик, идея | Формальное техническое задание |
| Не привязан к спринту | Всегда в спринте |
| Можно свободно редактировать | Следует строгому workflow (новая → в_работе → на_проверке → выполнена) |
| Любой сотрудник может добавлять | Создаёт только boss |
| Нет ролей (customer, developer, tester) | Есть роли и ответственные |
| Простая структура | Детализированная с Acceptance Criteria |

---

## 2. Структура записи бэклога

Каждая запись в бэклоге — объект JSON со следующими полями:

| Поле | Тип | Описание | Обязательное |
|------|-----|----------|--------------|
| `id` | string | Уникальный идентификатор (B001, B002...) | Да (генерируется автоматически) |
| `title` | string | Название идеи (краткое, до 100 символов) | Да |
| `description` | string | Подробное описание | Нет (может быть пустым) |
| `priority` | string | Приоритет: `high`, `medium`, `low` | Да (по умолчанию `medium`) |
| `next_sprint` | boolean | Флаг "В следующий спринт" | Да (по умолчанию `false`) |
| `created_by` | string | Кто добавил запись (имя сотрудника) | Да |
| `created_at` | string | Дата и время создания (ISO 8601) | Да (генерируется автоматически) |
| `updated_at` | string | Дата и время последнего изменения | Да (генерируется автоматически) |

### Формат JSON

Файл `.tayfa/common/backlog.json`:

```json
{
  "items": [
    {
      "id": "B001",
      "title": "Добавить экспорт отчётов в PDF",
      "description": "Пользователи просят возможность скачивать отчёты в PDF формате. Нужно исследовать библиотеки для генерации PDF.",
      "priority": "high",
      "next_sprint": true,
      "created_by": "analyst",
      "created_at": "2026-02-10T14:30:00Z",
      "updated_at": "2026-02-11T09:15:00Z"
    },
    {
      "id": "B002",
      "title": "Оптимизация загрузки больших списков",
      "description": "При 1000+ элементов интерфейс тормозит. Рассмотреть виртуальный скроллинг.",
      "priority": "medium",
      "next_sprint": false,
      "created_by": "developer_frontend",
      "created_at": "2026-02-11T10:00:00Z",
      "updated_at": "2026-02-11T10:00:00Z"
    }
  ],
  "next_id": 3
}
```

**Примечания:**
- `next_id` — счётчик для генерации следующего ID
- `items` — массив всех записей бэклога
- Записи сортируются по дате создания (новые сверху)

---

## 3. Кто может работать с бэклогом

### Права доступа

| Действие | Кто может выполнять |
|----------|---------------------|
| **Добавить запись** | Любой сотрудник |
| **Редактировать свою запись** | Автор записи |
| **Редактировать любую запись** | Boss |
| **Удалить запись** | Boss или автор записи |
| **Приоритизировать** | Boss |
| **Переносить в спринт** | Только boss |

### Поле created_by

Поле `created_by` отслеживает автора записи:
- Автоматически заполняется при создании
- Используется для фильтрации "мои идеи"
- Помогает boss понять, от кого пришла идея

**Пример:**
```bash
# Аналитик добавляет идею
python .tayfa/common/backlog_manager.py add "Новая фича" \
  --created-by analyst

# Boss видит, кто добавил
python .tayfa/common/backlog_manager.py list
# B001: Новая фича (analyst, high, 🔥 в следующий спринт)
```

---

## 4. Флаг "В следующий спринт"

### Назначение флага

Флаг `next_sprint` — это метка для записей, которые boss планирует включить в следующий спринт.

**Зачем нужен:**
- Boss просматривает бэклог перед созданием спринта
- Отмечает важные записи флагом `next_sprint`
- При создании спринта автоматически включаются отмеченные записи

### Как устанавливать/снимать

**CLI:**
```bash
# Переключить флаг (toggle)
python .tayfa/common/backlog_manager.py toggle B001

# Установить флаг явно
python .tayfa/common/backlog_manager.py edit B001 --next-sprint true

# Снять флаг
python .tayfa/common/backlog_manager.py edit B001 --next-sprint false
```

**UI (в оркестраторе):**
- Чекбокс "В следующий спринт" в карточке записи
- Клик по чекбоксу переключает флаг

### Как boss использует при планировании

**Workflow boss'а:**

1. **Перед спринтом** — просмотр бэклога:
   ```bash
   python .tayfa/common/backlog_manager.py list --priority high
   # Смотрит приоритетные записи
   ```

2. **Отметка записей для спринта**:
   ```bash
   python .tayfa/common/backlog_manager.py toggle B001
   python .tayfa/common/backlog_manager.py toggle B003
   python .tayfa/common/backlog_manager.py toggle B005
   ```

3. **Просмотр отмеченных**:
   ```bash
   python .tayfa/common/backlog_manager.py next-sprint
   # B001: Экспорт в PDF
   # B003: Интеграция с API
   # B005: Улучшение поиска
   ```

4. **Создание спринта с автоматическим включением**:
   ```bash
   python .tayfa/common/task_manager.py create-sprint \
     "Спринт 5: Экспорт и интеграции" \
     "Добавить экспорт отчётов и интеграцию с внешними API" \
     --include-backlog
   # → Автоматически создаются задачи из B001, B003, B005
   ```

### Визуальное отображение в UI

В оркестраторе записи с `next_sprint: true` отмечаются:
- 🔥 Иконка "огонь" перед названием
- Жирный шрифт
- Подсветка жёлтым/оранжевым цветом
- Отдельная секция "Для следующего спринта" на доске

---

## 5. Жизненный цикл записи

### Схема

```
┌─────────────────┐
│   Идея у        │
│   сотрудника    │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ 1. Добавление в бэклог                                  │
│    - Любой сотрудник добавляет через CLI или UI         │
│    - Заполняет: title, description, priority            │
│    - Автоматически: id, created_by, created_at          │
└────────┬────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ 2. Запись в бэклоге (хранение)                          │
│    - Видна в списке бэклога                             │
│    - Можно редактировать: title, description, priority  │
│    - Boss может изменить приоритет                      │
└────────┬────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Планирование спринта (опционально)                   │
│    - Boss просматривает бэклог                          │
│    - Отмечает записи флагом next_sprint                 │
│    - Запись остаётся в бэклоге                          │
└────────┬────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Создание задачи из бэклога                           │
│    - Boss переносит запись в спринт                     │
│    - Создаётся задача с ролями (customer/dev/tester)    │
│    - Запись удаляется из бэклога                        │
└────────┬────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│ 5. Задача в спринте                                     │
│    - Следует workflow: новая → в_работе → выполнена     │
│    - Из бэклога превратилась в формальную задачу        │
└─────────────────────────────────────────────────────────┘
```

### Детальное описание этапов

#### Этап 1: Добавление в бэклог

**Кто:** Любой сотрудник
**Когда:** Появилась идея, которую нужно зафиксировать

**Что происходит:**
1. Сотрудник выполняет команду или заполняет форму в UI
2. Генерируется уникальный ID (B001, B002...)
3. Устанавливается `created_by` — имя сотрудника
4. Устанавливаются временные метки
5. Запись сохраняется в `backlog.json`

**Пример:**
```bash
python .tayfa/common/backlog_manager.py add \
  "Добавить тёмную тему" \
  --description "Пользователи просят тёмную тему для работы ночью" \
  --priority medium
```

#### Этап 2: Хранение в бэклоге

**Кто:** Запись находится в бэклоге
**Когда:** От момента добавления до переноса в спринт

**Что происходит:**
- Запись видна в списке бэклога
- Boss периодически просматривает бэклог
- Можно редактировать (title, description, priority)
- Boss может изменить приоритет на более высокий
- Запись ждёт своей очереди

**Операции:**
```bash
# Просмотр
python .tayfa/common/backlog_manager.py list

# Редактирование
python .tayfa/common/backlog_manager.py edit B001 \
  --title "Новое название" \
  --priority high
```

#### Этап 3: Планирование спринта (опционально)

**Кто:** Boss
**Когда:** Перед созданием нового спринта

**Что происходит:**
1. Boss просматривает бэклог
2. Оценивает приоритеты и сроки
3. Отмечает записи для следующего спринта флагом `next_sprint`
4. Записи остаются в бэклоге, но помечены как "в планах"

**Операции:**
```bash
# Просмотр высокоприоритетных
python .tayfa/common/backlog_manager.py list --priority high

# Отметка для спринта
python .tayfa/common/backlog_manager.py toggle B001
python .tayfa/common/backlog_manager.py toggle B005

# Просмотр отмеченных
python .tayfa/common/backlog_manager.py next-sprint
```

#### Этап 4: Создание задачи из бэклога

**Кто:** Boss
**Когда:** Создание спринта или добавление задачи в существующий спринт

**Что происходит:**
1. Boss выбирает запись из бэклога
2. Создаётся формальная задача с ролями
3. Задача привязывается к спринту
4. **Запись удаляется из бэклога** (перенесена в задачу)

**Два способа:**

**Способ А — автоматически при создании спринта:**
```bash
python .tayfa/common/task_manager.py create-sprint \
  "Спринт 6" "Описание" \
  --include-backlog

# → Все записи с next_sprint=true автоматически превращаются в задачи
# → Записи удаляются из бэклога
```

**Способ Б — вручную для отдельной записи:**
```bash
python .tayfa/common/task_manager.py create-from-backlog B001 \
  --customer analyst \
  --developer developer_python \
  --tester qa_tester \
  --sprint S005

# → Создана задача T023 из записи B001
# → B001 удалена из бэклога
```

#### Этап 5: Задача в спринте

**Кто:** Задача теперь в `tasks.json`
**Когда:** После переноса из бэклога

**Что происходит:**
- Запись из бэклога стала формальной задачей
- Назначены роли: постановщик, разработчик, тестировщик
- Задача следует workflow: новая → в_работе → на_проверке → выполнена
- Обсуждения ведутся в `discussions/{task_id}.md` (файл создаётся автоматически при создании задачи)

**Задача больше не связана с бэклогом** — это независимая сущность.

---

## 6. CLI команды (backlog_manager.py)

Все операции с бэклогом выполняются через `.tayfa/common/backlog_manager.py`.

### 6.1 add — Добавить запись

```bash
python .tayfa/common/backlog_manager.py add <title> \
  [--description <text>] \
  [--priority high|medium|low] \
  [--created-by <name>]
```

**Параметры:**
- `title` — название идеи (обязательно)
- `--description` — подробное описание (опционально)
- `--priority` — приоритет: `high`, `medium`, `low` (по умолчанию `medium`)
- `--created-by` — имя автора (по умолчанию текущий агент)

**Примеры:**
```bash
# Минимальная команда
python .tayfa/common/backlog_manager.py add "Добавить поиск"

# С описанием и приоритетом
python .tayfa/common/backlog_manager.py add "Экспорт в Excel" \
  --description "Пользователи просят экспорт таблиц в Excel формат" \
  --priority high

# С указанием автора
python .tayfa/common/backlog_manager.py add "Оптимизация БД" \
  --description "Запросы выполняются медленно при большом объёме данных" \
  --priority medium \
  --created-by developer_backend
```

**Вывод:**
```
✅ Добавлено в бэклог: B003
   Название: Экспорт в Excel
   Приоритет: high
   Автор: analyst
```

### 6.2 list — Список записей

```bash
python .tayfa/common/backlog_manager.py list \
  [--priority high|medium|low] \
  [--next-sprint] \
  [--created-by <name>]
```

**Параметры:**
- `--priority` — фильтр по приоритету
- `--next-sprint` — только записи с флагом next_sprint=true
- `--created-by` — только записи конкретного автора

**Примеры:**
```bash
# Все записи
python .tayfa/common/backlog_manager.py list

# Только высокоприоритетные
python .tayfa/common/backlog_manager.py list --priority high

# Только для следующего спринта
python .tayfa/common/backlog_manager.py list --next-sprint

# Идеи конкретного сотрудника
python .tayfa/common/backlog_manager.py list --created-by analyst
```

**Вывод:**
```
Бэклог задач (3 записи):

B001: Экспорт отчётов в PDF [high] 🔥
      Автор: analyst | Создано: 2026-02-10

B002: Оптимизация загрузки списков [medium]
      Автор: developer_frontend | Создано: 2026-02-11

B003: Интеграция с Telegram [low]
      Автор: product_manager | Создано: 2026-02-11
```

### 6.3 get — Получить одну запись

```bash
python .tayfa/common/backlog_manager.py get <backlog_id>
```

**Пример:**
```bash
python .tayfa/common/backlog_manager.py get B001
```

**Вывод:**
```json
{
  "id": "B001",
  "title": "Экспорт отчётов в PDF",
  "description": "Пользователи просят возможность скачивать отчёты в PDF формате...",
  "priority": "high",
  "next_sprint": true,
  "created_by": "analyst",
  "created_at": "2026-02-10T14:30:00Z",
  "updated_at": "2026-02-11T09:15:00Z"
}
```

### 6.4 edit — Редактировать запись

```bash
python .tayfa/common/backlog_manager.py edit <backlog_id> \
  [--title <new_title>] \
  [--description <new_description>] \
  [--priority high|medium|low] \
  [--next-sprint true|false]
```

**Примеры:**
```bash
# Изменить название
python .tayfa/common/backlog_manager.py edit B001 \
  --title "Экспорт в PDF и Excel"

# Изменить приоритет
python .tayfa/common/backlog_manager.py edit B001 --priority high

# Изменить несколько полей
python .tayfa/common/backlog_manager.py edit B001 \
  --title "Новое название" \
  --description "Новое описание" \
  --priority medium \
  --next-sprint true
```

**Вывод:**
```
✅ Запись B001 обновлена
```

### 6.5 toggle — Переключить флаг next_sprint

```bash
python .tayfa/common/backlog_manager.py toggle <backlog_id>
```

**Пример:**
```bash
# Если next_sprint=false → станет true
# Если next_sprint=true → станет false
python .tayfa/common/backlog_manager.py toggle B001
```

**Вывод:**
```
✅ B001: Флаг next_sprint = true
```

### 6.6 remove — Удалить запись

```bash
python .tayfa/common/backlog_manager.py remove <backlog_id>
```

**Пример:**
```bash
python .tayfa/common/backlog_manager.py remove B001
```

**Вывод:**
```
✅ Запись B001 удалена из бэклога
```

**Внимание:** Удаление необратимо. Используйте осторожно.

### 6.7 next-sprint — Показать записи для следующего спринта

```bash
python .tayfa/common/backlog_manager.py next-sprint
```

Эквивалентно `list --next-sprint`.

**Вывод:**
```
Записи для следующего спринта (2):

B001: Экспорт отчётов в PDF [high]
B005: Улучшение поиска [medium]
```

---

## 7. Интеграция с task_manager

Для переноса записей бэклога в задачи используются команды `task_manager.py`.

### 7.1 create-from-backlog — Создать задачу из записи бэклога

```bash
python .tayfa/common/task_manager.py create-from-backlog <backlog_id> \
  --customer <name> \
  --developer <name> \
  --tester <name> \
  --sprint <sprint_id>
```

**Параметры:**
- `backlog_id` — ID записи в бэклоге (B001, B002...)
- `--customer` — постановщик задачи
- `--developer` — разработчик
- `--tester` — тестировщик
- `--sprint` — ID спринта, в который добавить задачу

**Что происходит:**
1. Извлекается запись из бэклога по ID
2. Создаётся задача с названием и описанием из бэклога
3. Назначаются роли
4. Задача привязывается к спринту
5. **Запись удаляется из бэклога**

**Пример:**
```bash
python .tayfa/common/task_manager.py create-from-backlog B001 \
  --customer analyst \
  --developer developer_python \
  --tester qa_tester \
  --sprint S005
```

**Вывод:**
```
✅ Создана задача T023 из бэклога B001
   Название: Экспорт отчётов в PDF
   Спринт: S005
   Постановщик: analyst
   Разработчик: developer_python
   Тестировщик: qa_tester

ℹ️  Запись B001 удалена из бэклога
```

### 7.2 create-sprint --include-backlog — Автоматически включить бэклог

```bash
python .tayfa/common/task_manager.py create-sprint <title> <description> \
  --include-backlog
```

**Параметры:**
- `title` — название спринта
- `description` — описание спринта
- `--include-backlog` — автоматически создать задачи из записей с `next_sprint=true`

**Что происходит:**
1. Создаётся спринт
2. Находятся все записи бэклога с `next_sprint=true`
3. Для каждой записи:
   - Создаётся задача в спринте
   - Назначаются роли (по умолчанию или из настроек)
   - Запись удаляется из бэклога
4. Создаётся задача "Финализировать спринт"

**Пример:**
```bash
# Предварительно отметить записи
python .tayfa/common/backlog_manager.py toggle B001
python .tayfa/common/backlog_manager.py toggle B003
python .tayfa/common/backlog_manager.py toggle B005

# Создать спринт с автоматическим включением
python .tayfa/common/task_manager.py create-sprint \
  "Спринт 6: Экспорт и интеграции" \
  "Добавить функции экспорта и интеграцию с внешними сервисами" \
  --include-backlog
```

**Вывод:**
```
✅ Создан спринт S006: Спринт 6: Экспорт и интеграции

✅ Создано 3 задачи из бэклога:
   T024: Экспорт отчётов в PDF (из B001)
   T025: Интеграция с Telegram API (из B003)
   T026: Улучшение поиска (из B005)

✅ Создана задача финализации: T027

ℹ️  Удалено из бэклога: B001, B003, B005
```

**Назначение ролей по умолчанию:**
- `customer` — первый аналитик из `employees.json` или `boss`
- `developer` — первый разработчик из `employees.json` или `boss`
- `tester` — первый тестировщик из `employees.json` или `boss`

Boss может переназначить роли после создания задач.

---

## 8. Примеры использования

### Сценарий 1: Analyst добавляет идею

**Контекст:** Аналитик изучает конкурентов и находит интересную фичу.

```bash
# 1. Analyst фиксирует идею в бэклог
python .tayfa/common/backlog_manager.py add \
  "Добавить фильтры по датам в отчётах" \
  --description "Конкуренты предлагают гибкие фильтры: последние 7 дней, текущий месяц, произвольный период. Нашим пользователям это тоже нужно." \
  --priority medium \
  --created-by analyst

# 2. Boss видит новую запись
python .tayfa/common/backlog_manager.py list
# B007: Добавить фильтры по датам в отчётах [medium]
#       Автор: analyst | Создано: 2026-02-13
```

**Результат:** Идея зафиксирована, не потеряется.

### Сценарий 2: Boss просматривает и приоритизирует

**Контекст:** Boss готовится к планированию следующего спринта.

```bash
# 1. Просмотр всех записей
python .tayfa/common/backlog_manager.py list

# 2. Просмотр только высокоприоритетных
python .tayfa/common/backlog_manager.py list --priority high

# 3. Boss решает, что фильтры по датам важны
python .tayfa/common/backlog_manager.py edit B007 --priority high

# 4. Отмечает для следующего спринта
python .tayfa/common/backlog_manager.py toggle B007

# 5. Проверяет, что отмечено
python .tayfa/common/backlog_manager.py next-sprint
# B007: Добавить фильтры по датам в отчётах [high] 🔥
```

**Результат:** Запись приоритизирована и подготовлена к переносу в спринт.

### Сценарий 3: Создание спринта с автоматическим включением бэклога

**Контекст:** Boss создаёт новый спринт и хочет автоматически включить отмеченные записи.

```bash
# 1. Boss отметил несколько записей (из предыдущих шагов)
python .tayfa/common/backlog_manager.py next-sprint
# B001: Экспорт в PDF [high] 🔥
# B007: Фильтры по датам [high] 🔥
# B009: Уведомления на email [medium] 🔥

# 2. Создание спринта с автоматическим включением
python .tayfa/common/task_manager.py create-sprint \
  "Спринт 7: Отчёты и уведомления" \
  "Улучшить функции отчётов: экспорт, фильтры, уведомления" \
  --include-backlog

# 3. Результат
# ✅ Создан спринт S007
# ✅ Создано 3 задачи:
#    T030: Экспорт в PDF (из B001)
#    T031: Фильтры по датам (из B007)
#    T032: Уведомления на email (из B009)
# ✅ Создана задача финализации: T033
# ℹ️  Удалено из бэклога: B001, B007, B009

# 4. Проверка бэклога
python .tayfa/common/backlog_manager.py list
# (B001, B007, B009 больше нет — перенесены в задачи)
```

**Результат:** Спринт создан, задачи автоматически добавлены, бэклог очищен от перенесённых записей.

### Сценарий 4: Перенос отдельной записи в существующий спринт

**Контекст:** Boss решает добавить ещё одну задачу в текущий спринт из бэклога.

```bash
# 1. Boss видит в бэклоге интересную запись
python .tayfa/common/backlog_manager.py get B010
# B010: Интеграция с Google Calendar
# Описание: Синхронизация событий с Google Calendar

# 2. Решает добавить в текущий спринт S007
python .tayfa/common/task_manager.py create-from-backlog B010 \
  --customer analyst \
  --developer developer_python \
  --tester qa_tester \
  --sprint S007

# 3. Результат
# ✅ Создана задача T034 из бэклога B010
#    Спринт: S007
# ℹ️  Запись B010 удалена из бэклога

# 4. Проверка задач спринта
python .tayfa/common/task_manager.py list --sprint S007
# T030, T031, T032, T033, T034 (новая задача из бэклога)
```

**Результат:** Задача добавлена в спринт вручную, без пересоздания спринта.

---

## 9. Правила и рекомендации

### Когда использовать бэклог vs сразу создать задачу

| Используй бэклог | Создавай задачу сразу |
|------------------|-----------------------|
| Идея ещё не проработана | Чёткие требования уже есть |
| Нужно обсудить с командой | Срочная задача, нужна прямо сейчас |
| Функционал для будущих спринтов | Часть текущего спринта |
| "Хорошо бы сделать когда-нибудь" | "Нужно сделать в этом спринте" |
| Требуется исследование | Техническое задание готово |

**Примеры:**

✅ **В бэклог:**
- "Было бы круто добавить тёмную тему"
- "Подумать об интеграции с Slack"
- "Оптимизировать производительность (исследовать узкие места)"

✅ **Сразу в задачу:**
- "Исправить баг: кнопка 'Сохранить' не работает на странице настроек"
- "Добавить валидацию email в форме регистрации (критерии готовы)"
- "Обновить версию библиотеки X из-за уязвимости"

### Как приоритизировать записи

**Критерии приоритета:**

| Приоритет | Критерии | Примеры |
|-----------|----------|---------|
| **high** | Важно для бизнеса, запрашивают пользователи, блокирует другие фичи | Экспорт данных, критичная интеграция, исправление UX-проблем |
| **medium** | Полезно, но не срочно, улучшает опыт | Оптимизация производительности, новые фильтры, удобство интерфейса |
| **low** | "Nice to have", можно отложить | Мелкие улучшения, экспериментальные фичи, косметические изменения |

**Рекомендация boss'у:**
- Раз в неделю просматривай бэклог
- Переоценивай приоритеты (может low стал high)
- Удаляй устаревшие записи (уже не актуально)

### Как формулировать описания в бэклоге

**Хорошие формулировки:**

✅ **Краткое название:**
- "Добавить экспорт в PDF"
- "Интеграция с Stripe для оплаты"
- "Оптимизация загрузки больших списков"

✅ **Подробное описание:**
- Контекст: почему это нужно
- Что хотим получить в итоге
- Примеры или референсы (если есть)

**Пример хорошей записи:**
```
Название: Добавить экспорт отчётов в PDF

Описание:
Пользователи просят возможность скачивать отчёты в PDF формате для печати
и отправки клиентам. Сейчас можно только просматривать на экране.

Референс: как сделано в конкуренте Acme Reports — кнопка "Скачать PDF"
генерирует красиво оформленный документ с логотипом компании.

Потребуется: исследовать библиотеки для генерации PDF (ReportLab, WeasyPrint).
```

**Плохие формулировки:**

❌ **Слишком расплывчато:**
- "Улучшить интерфейс" (что именно?)
- "Сделать быстрее" (что именно ускорить?)
- "Добавить фичу" (какую?)

❌ **Слишком технично для бэклога:**
- "Рефакторинг модуля auth.py с использованием паттерна Strategy"
  (это уже техническая задача, не идея для бэклога)

**Золотое правило:**
Название — что хотим получить (для пользователя).
Описание — почему это нужно + контекст.

### Работа с устаревшими записями

Бэклог может зарастать устаревшими идеями. **Регулярная чистка:**

```bash
# Раз в месяц boss проверяет старые записи
python .tayfa/common/backlog_manager.py list

# Удаляет то, что уже не актуально
python .tayfa/common/backlog_manager.py remove B005  # уже реализовали другим способом
python .tayfa/common/backlog_manager.py remove B012  # передумали, не нужно

# Или изменяет приоритет
python .tayfa/common/backlog_manager.py edit B020 --priority low  # отложить
```

**Критерии для удаления:**
- Идея уже реализована другим способом
- Требования изменились, идея больше не актуальна
- Фича оказалась невостребованной (после исследования)
- Техническая невозможность или слишком дорого

---

## 10. Связь с другими документами

**См. также:**
- `.tayfa/common/Rules/teamwork.md` — общий workflow команды, секция "Бэклог"
- `.tayfa/common/Rules/git-workflow.md` — как коммитятся изменения при финализации спринта
- `.tayfa/common/Rules/employees.md` — роли сотрудников для назначения на задачи

**API документация:**
- `backlog_manager.py` — CLI для работы с бэклогом
- `task_manager.py` — CLI для создания задач и спринтов
- `backlog.json` — формат хранения данных

---

## Итоги

**Бэклог — это:**
- 📝 Копилка идей и будущих задач
- 🎯 Инструмент планирования для boss'а
- 🔄 Промежуточное звено: идея → бэклог → задача → спринт
- 👥 Доступен для добавления всем сотрудникам
- 🚀 Автоматизирует создание спринтов из отмеченных записей

**Основной workflow:**
1. Сотрудник фиксирует идею в бэклог
2. Boss периодически просматривает, приоритизирует
3. Boss отмечает записи для следующего спринта
4. Boss создаёт спринт с автоматическим включением бэклога
5. Записи превращаются в задачи, удаляются из бэклога

**Ключевые команды:**
- `add` — добавить идею
- `list` — просмотреть бэклог
- `toggle` — отметить для спринта
- `create-sprint --include-backlog` — создать спринт из бэклога

Используй бэклог для фиксации всех идей — ничего не потеряется! 🎯
